# ── 크롤링 / 수집 ────────────────────────────────────────────────
scrapy==2.12.0
Twisted>=22.10.0,<24.11.0
feedparser==6.0.11
newspaper4k==0.9.3.1        # newspaper3k 대체 (Python 3.11+ 호환)
beautifulsoup4==4.12.3
lxml==5.2.2

# ── 뉴스 API 클라이언트 ───────────────────────────────────────────
newsapi-python==0.2.7       # NewsAPI.org 공식 클라이언트
pytrends==4.9.2             # Google Trends 비공식 클라이언트

# ── DB ───────────────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1
redis==5.0.8

# ── 데이터 처리 ───────────────────────────────────────────────────
pandas==2.2.2               # 집계 파이프라인 데이터 처리

# ── 분류 / 중복 제거 ──────────────────────────────────────────────
scikit-learn==1.5.0
kiwipiepy==0.18.0           # 한국어 형태소 분석
datasketch==1.6.4           # LSH MinHash 기반 중복 감지

# ── 주식 데이터 ───────────────────────────────────────────────────
pykrx==1.0.47                # 한국거래소(KRX) 주가 데이터 수집

# ── 검색 ─────────────────────────────────────────────────────────
elasticsearch==8.14.0
sentence-transformers==3.0.0  # RAG 벡터 검색용 다국어 문장 임베딩 (BE-AI와 동일 버전)

# ── HTTP ─────────────────────────────────────────────────────────
httpx==0.27.0

# ── 유틸 ─────────────────────────────────────────────────────────
python-dotenv==1.0.1